const name1 = "Jonathan Price"
const name2 = "Jonathan Longer"

if (name1.length > name2.length){
console.log (`${name1} has the longer name.`);
} else if (name2.length > name1.length){
console.log (`${name2} has the longer name.`);
}